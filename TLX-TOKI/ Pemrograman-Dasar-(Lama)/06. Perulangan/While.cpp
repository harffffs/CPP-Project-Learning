#include<bits/stdc++.h>
using namespace std;

int main() {
    //Deklarasi Variabel
    string text;

    //Input
    while(getline(cin, text)) {
        //Output
        cout << text << endl;
    }
}
